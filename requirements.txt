
"""Use requirements.txt to install dependencies via pip if torch via uv does not work for your system."""

# Dev dependencies (optional, can install separately)
coverage==7.6.9
# Core dependencies (match uv pyproject.toml)
fastapi==0.115.6
invoke==2.2.0
mkdocs==1.6.1
mkdocs-material==9.4.6
mkdocstrings-python==1.12.2
pre-commit==4.1.0
pytest==8.3.4
ruff==0.1.3
typer==0.15.1
uvicorn==0.34.0

# NOTE: PyTorch cannot be installed via pip on Intel macOS
# Install via Conda manually:
# conda install pytorch cpuonly -c pytorch
